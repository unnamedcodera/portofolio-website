# Environment variables for Docker Compose
# Copy this file to .env and update with your values

# JWT Secret (use a strong random string)
JWT_SECRET=your-super-secret-jwt-key-at-least-64-characters-long-change-in-production

# CSRF Secret (use a strong random string)
CSRF_SECRET=your-csrf-secret-key-at-least-64-characters-long-change-in-production

# Node Environment
NODE_ENV=production

# CORS Origin (your frontend domain)
CORS_ORIGIN=http://localhost

# Database path (optional, default: /app/database.db)
# DATABASE_PATH=/app/database.db
